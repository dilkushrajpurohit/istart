<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Purchase - S Commerce</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .container-custom {
            max-width: 900px;
        }

        .card-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
            border-radius: 15px 15px 0 0;
        }

        .card-header h1 {
            margin: 0;
            font-weight: 700;
            font-size: 28px;
        }

        .card-header p {
            margin: 10px 0 0 0;
            opacity: 0.9;
        }

        .card {
            border: none;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            border-radius: 15px;
            overflow: hidden;
        }

        .card-body {
            padding: 40px;
        }

        .product-details {
            background: #f8f9fa;
            padding: 25px;
            border-radius: 12px;
            margin-bottom: 30px;
            border-left: 4px solid #667eea;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #e9ecef;
        }

        .detail-row:last-child {
            border-bottom: none;
        }

        .detail-label {
            font-weight: 600;
            color: #555;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .detail-value {
            color: #333;
            font-size: 16px;
            font-weight: 600;
        }

        .product-name {
            font-size: 24px !important;
            color: #667eea !important;
        }

        .product-price {
            font-size: 28px !important;
            color: #28a745 !important;
        }

        .seller-info, .buyer-info {
            background: white;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
            border: 1px solid #e9ecef;
        }

        .info-title {
            font-weight: 700;
            color: #333;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid #667eea;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .info-item {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 10px;
            font-size: 14px;
        }

        .info-item i {
            color: #667eea;
            width: 20px;
        }

        .info-item-value {
            color: #555;
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }

        .btn-confirm {
            flex: 1;
            padding: 15px;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            text-align: center;
        }

        .btn-confirm-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-confirm-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
            color: white;
        }

        .btn-confirm-secondary {
            background: #f8f9fa;
            color: #333;
            border: 2px solid #ddd;
        }

        .btn-confirm-secondary:hover {
            background: #e9ecef;
            color: #333;
            text-decoration: none;
        }

        .form-mobile {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 25px;
        }

        .form-mobile label {
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
            display: block;
        }

        .form-mobile input {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 6px;
            font-size: 16px;
        }

        .form-mobile input:focus {
            border-color: #667eea;
            outline: none;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .warning-message {
            background: #fff3cd;
            padding: 15px;
            border-radius: 8px;
            border-left: 4px solid #ffc107;
            margin-bottom: 25px;
            color: #856404;
            font-size: 14px;
            display: flex;
            gap: 10px;
            align-items: flex-start;
        }

        .warning-message i {
            margin-top: 2px;
            flex-shrink: 0;
        }

        @media (max-width: 768px) {
            .card-body {
                padding: 25px;
            }

            .action-buttons {
                flex-direction: column;
            }

            .product-details {
                padding: 15px;
            }

            .detail-row {
                flex-direction: column;
                align-items: flex-start;
            }

            .card-header h1 {
                font-size: 22px;
            }
        }
    </style>
</head>
<body>
    <div class="container-custom">
        <div class="card">
            <div class="card-header">
                <h1><i class="fas fa-shopping-cart"></i> Confirm Your Purchase</h1>
                <p>Review the details before proceeding</p>
            </div>

            <div class="card-body">
                <!-- Product Details -->
                <div class="product-details">
                    <div class="detail-row">
                        <span class="detail-label">Product Name</span>
                        <span class="detail-value product-name"><%= product.name %></span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Price</span>
                        <span class="detail-value product-price">₹<%= product.price %></span>
                    </div>
                    <div class="detail-row">
                        <span class="detail-label">Description</span>
                        <span class="detail-value"><%= product.Desc %></span>
                    </div>
                </div>

                <!-- Seller Information -->
                <div class="seller-info">
                    <div class="info-title">
                        <i class="fas fa-store"></i> Seller Information
                    </div>
                    <div class="info-item">
                        <i class="fas fa-user"></i>
                        <span class="info-item-value"><strong><%= seller.name %></strong> (Seller)</span>
                    </div>
                    <div class="info-item">
                        <i class="fas fa-envelope"></i>
                        <span class="info-item-value"><%= seller.email %></span>
                    </div>
                    <div class="info-item">
                        <i class="fas fa-phone"></i>
                        <span class="info-item-value">+91 <%= product.mobile %></span>
                    </div>
                </div>

                <!-- Buyer Information -->
                <div class="buyer-info">
                    <div class="info-title">
                        <i class="fas fa-user-check"></i> Your Information
                    </div>
                    <div class="info-item">
                        <i class="fas fa-user"></i>
                        <span class="info-item-value"><strong><%= buyer %></strong> (Buyer)</span>
                    </div>
                    <div class="info-item">
                        <i class="fas fa-envelope"></i>
                        <span class="info-item-value"><%= buyerEmail %></span>
                    </div>
                </div>

                <!-- Mobile Number Form -->
                <div class="form-mobile">
                    <label for="mobile"><i class="fas fa-phone-alt"></i> Your Mobile Number (for seller contact)</label>
                    <input type="tel" id="mobile" name="mobile" placeholder="Enter your 10-digit mobile number" 
                        pattern="[0-9]{10}" required>
                    <small style="color: #666; margin-top: 5px; display: block;">
                        <i class="fas fa-info-circle"></i> The seller will contact you on this number
                    </small>
                </div>

                <!-- Warning Message -->
                <div class="warning-message">
                    <i class="fas fa-exclamation-triangle"></i>
                    <div>
                        <strong>Important:</strong> You will be redirected to WhatsApp to contact the seller. 
                        Make sure you have WhatsApp installed on your device.
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="action-buttons">
                    <a href="/home" class="btn-confirm btn-confirm-secondary">
                        <i class="fas fa-arrow-left"></i> Back to Shopping
                    </a>
                    <form action="/confirmbuy/<%= product._id %>" method="POST" style="flex: 1;">
                        <input type="hidden" name="mobile" id="mobileHidden" value="">
                        <button type="submit" class="btn-confirm btn-confirm-primary" style="width: 100%;">
                            <i class="fas fa-check-circle"></i> Confirm & Contact Seller on WhatsApp
                        </button>
                    </form>
                </div>

                <script>
                    document.querySelectorAll('button[type="submit"]')[0]?.addEventListener('click', function(e) {
                        const mobile = document.getElementById('mobile').value;
                        if(!mobile || mobile.length !== 10) {
                            e.preventDefault();
                            alert('Please enter a valid 10-digit mobile number');
                            return;
                        }
                        document.getElementById('mobileHidden').value = mobile;
                    });
                </script>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
